# =============================================================================
# AlfaWall Conversation API - Minimal Skeleton Configuration
# =============================================================================
# This is a bare-bones configuration for the skeleton REST API.
# Add your own configuration as you implement features.
# =============================================================================

spring:
  application:
    name: alfa-wall-addon

# Server configuration
server:
  port: 8080

# Logging
logging:
  level:
    root: INFO
    nl.alfaone: DEBUG
