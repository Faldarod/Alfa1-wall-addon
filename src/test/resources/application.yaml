
# Environment-based configuration keys
homeassistant:
  base-url: http://home-assistant:8123
  access-token: no-token
# MCP (Model Context Protocol) Server Configuration - Test Environment
mcp:
  enabled: false  # Disabled by default in tests
  servers: []
